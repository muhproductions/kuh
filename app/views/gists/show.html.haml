%div.container
  - @gist['snippets'].each_with_index do |(k,v),i|
    %div.jumbotron{style: 'padding: 0;'}
      %textarea.hero-spacer.z-depth-2{id: "gist-#{i}"}
        = preserve do
          =v['paste']
      %div.snippet-desc
        %h3
          %span.label.label-danger
            = v['lang'].capitalize
        %button.btn.btn-danger.btn-raised.copy-codemirror{target: "#gist-#{i}"}
          %i.material-icons
            content_copy
    :javascript
      this.viewer($("#gist-#{i}")[0], "#{v['lang']}");
